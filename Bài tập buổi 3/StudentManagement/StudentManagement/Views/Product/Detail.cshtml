@{
    ViewData["Title"] = "Product Detail";
}

<h2>Product Detail</h2>

@if (ViewBag.IsError == true)
{
    <div class="alert alert-danger">@ViewBag.Message</div>
}
else
{
    <p class="lead">@ViewBag.Message</p>
}

<p class="text-muted">Ví dụ URL: /Product/Detail/5</p>
